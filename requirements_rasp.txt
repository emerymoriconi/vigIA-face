# --- CRÍTICO: TRAVAS DE VERSÃO (Raspberry Pi 5 / Bookworm) ---
# Mantém compatibilidade com o picamera2 do sistema.
# Se atualizar para 2.0.0, a câmera para de funcionar.
numpy>=1.26.0,<2.0.0

# O OpenCV deve ser < 4.10 para aceitar o NumPy antigo (<2).
opencv-python<4.10.0

# --- INTELIGÊNCIA ARTIFICIAL E VISÃO ---
# YOLO (Ultralytics). Ele instalará PyTorch e TorchVision.
# Graças às travas acima, ele baixará versões compatíveis.
ultralytics

# Dlib (Necessário compilar ou instalar via apt antes).
# Se já estiver instalado no sistema, o pip apenas verificará.
dlib

# Wrapper fácil para reconhecimento facial usando dlib
face_recognition

# --- UTILITÁRIOS E HARDWARE ---
psutil>=5.9.0           # Monitoramento de CPU/RAM
requests>=2.31.0        # Comunicação HTTP/API
python-dotenv>=1.0.0    # Leitura de arquivos .env

# --- PACOTES DO SISTEMA (NÃO INCLUIR NESTE ARQUIVO) ---
# Estes pacotes são gerenciados pelo 'apt' e herdados via 
# --system-site-packages. Não tente instalá-los via pip.
# --------------------------------------------------------
# picamera2  (Vem do pacote python3-picamera2)
# Pillow     (Vem do pacote python3-pil.imagetk)